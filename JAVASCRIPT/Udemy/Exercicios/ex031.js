const arr = [1,2,3,4]
const arre = [1,2,3,4,5,6,7,8]

function verifica(x) {
    if (x.length < 5 ) {
        console.log('Poucos elementos')
    } else { console.log('Muitos elementos')}
}

verifica(arr)
verifica(arre)

